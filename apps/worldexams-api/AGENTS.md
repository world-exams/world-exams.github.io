# WorldExams API Worker Agent Delta

This file is a local delta for `apps/worldexams-api/`.
Root governance still lives in `/AGENTS.md`.

## Scope

Edit this package for:

- Cloudflare Worker gateway code
- Wrangler config
- worker-level routing and API enforcement

Do not implement here:

- product pages
- site marketing content
- shared exam runtime UI

If a change is product-facing UI/runtime work, move to `saberparatodos/`.
